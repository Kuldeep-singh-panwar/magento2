<?php
$currentUrl = $block->getUrl('*/*/*');
?>
<div class="custom-sidebar">
    <style>
        .custom-sidebar { background: #2c3e50; min-height: 400px; padding: 20px 0; }
        .sidebar-menu { list-style: none; padding: 0; margin: 0; }
        .sidebar-menu li { padding: 12px 20px; border-left: 4px solid transparent; }
        .sidebar-menu li:hover { background: #34495e; border-left-color: #3498db; }
        .sidebar-menu li.active { background: #34495e; border-left-color: #3498db; }
        .sidebar-menu a { color: white; text-decoration: none; display: block; font-size: 14px; }
    </style>
    
    <ul class="sidebar-menu">
        <li class="<?= (strpos($currentUrl, 'setting') !== false) ? 'active' : '' ?>">
            <a href="<?= $block->getUrl('dashboard/index/setting') ?>">⚙️ Setting</a>
        </li>
        <li class="active">
            <a href="<?= $block->getUrl('dashboard/index/products') ?>">📦 Products & Videos</a>
        </li>
        <li class="<?= (strpos($currentUrl, 'info') !== false) ? 'active' : '' ?>">
            <a href="<?= $block->getUrl('dashboard/index/info') ?>">ℹ️ Info</a>
        </li>
    </ul>
</div>